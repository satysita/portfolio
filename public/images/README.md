# Images

Place your application images in this directory.
You can reference them in your code using absolute paths starting with `/images/`.

Example:
If you have `myapp/public/images/logo.png`, you can use it in your component like this:

```tsx
<img src="/images/logo.png" alt="Logo" />
```
